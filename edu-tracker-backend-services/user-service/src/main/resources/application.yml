server:
    port: 8082
spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/userdb
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

internal:
  secret: my-ultra-secure-internal-key
